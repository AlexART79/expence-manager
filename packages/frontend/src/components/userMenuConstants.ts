export const USER_MENU_COPY = {
  logout: "Log out",
  avatarAlt: (displayName: string) => `${displayName} avatar`,
  initialsLabel: (displayName: string) => `${displayName} initials`
} as const;

export const USER_MENU_IMAGE = {
  referrerPolicy: "no-referrer"
} as const;
